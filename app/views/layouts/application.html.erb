<!DOCTYPE html>
<html>
<head>
    <title>SSTaskTracking</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>

    <script>
        <% if user_signed_in? %>
            var current_user_content = {
                id: <%= current_user.id %>,
                first_name: "<%= current_user.first_name %>",
                last_name: "<%= current_user.last_name%>"
            };
        <% end %>
    </script>

    <!-- Place favicon.ico and apple-touch-icon.png in the root directory -->
    <% if user_signed_in? %>
        <link rel="stylesheet" href="app/styles/c828a93a.main.css">

        <script src="app/scripts/vendor/81b1549c.modernizr.js"></script>
  
         <script src="app/scripts/284cd242.vendor.js"></script>
    
        <script src="app/scripts/3481ab3a.main.js"></script>
    <% end %>

</head>
<body>

<%= yield %>

</body>
</html>